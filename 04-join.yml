---
- name: Join worker nodes to Kubernetes cluster
  hosts: k8s_workers
  become: yes
  vars:
    master_node: "{{ groups['k8s_master'][0] }}"

  tasks:
    - name: Read join command from master node
      slurp:
        src: /tmp/k8s_join_command.txt
      register: join_command_file
      delegate_to: "{{ master_node }}"
      run_once: yes

    # 1. Принудительно задаем IP для kubelet (через файл настроек)
    - name: Configure Kubelet Node IP
      copy:
        dest: /etc/default/kubelet
        content: "KUBELET_EXTRA_ARGS=--node-ip={{ ansible_host }}"

    # 2. Перезапускаем kubelet, чтобы он увидел настройку
    - name: Restart kubelet
      systemd:
        name: kubelet
        state: restarted

    # 3. Теперь выполняем join (БЕЗ лишних флагов)
    - name: Join worker to cluster
      shell: "{{ join_command_file['content'] | b64decode }}"
      args:
        creates: /etc/kubernetes/kubelet.conf

    - name: Wait for node to register (kubelet config)
      wait_for:
        path: /etc/kubernetes/kubelet.conf
        timeout: 60

    - name: Verify cluster nodes from master
      shell: kubectl get nodes -o wide
      environment:
        KUBECONFIG: /etc/kubernetes/admin.conf
      register: cluster_nodes
      delegate_to: "{{ master_node }}"
      run_once: yes
      changed_when: false

    - name: Display current cluster status
      debug:
        msg: "{{ cluster_nodes.stdout_lines }}"
